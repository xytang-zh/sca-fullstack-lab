<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xytang.article.mapper.ArticleMapper">

    <!-- 已发布文章分页：sort=hot 按加权热度降序，否则按发布时间倒序 -->
    <select id="selectPublishedPage" resultType="com.xytang.article.vo.ArticleVO">
        SELECT
            CAST(id AS CHAR) AS id,
            title,
            summary,
            cover_image AS coverImage,
            CAST(author_id AS CHAR) AS authorId,
            views,
            likes,
            favorites,
            comments,
            publish_time AS publishTime
        FROM t_article
        WHERE deleted = 0 AND status = 3
        <choose>
            <when test="sort == 'hot'">
                ORDER BY (CAST(#{wViews} AS DECIMAL(10, 2)) * views
                        + CAST(#{wLikes} AS DECIMAL(10, 2)) * likes
                        + CAST(#{wFav} AS DECIMAL(10, 2)) * favorites
                        + CAST(#{wComm} AS DECIMAL(10, 2)) * comments) DESC
            </when>
            <otherwise>
                ORDER BY publish_time DESC
            </otherwise>
        </choose>
    </select>

    <!-- 互动计数 +1（column 仅用于 choose 判断，不拼接进 SQL） -->
    <update id="incrCount">
        UPDATE t_article
        SET
        <choose>
            <when test="column == 'likes'">likes = likes + 1</when>
            <when test="column == 'favorites'">favorites = favorites + 1</when>
            <otherwise>likes = likes</otherwise>
        </choose>
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 互动计数 -1（下限 0） -->
    <update id="decrCount">
        UPDATE t_article
        SET
        <choose>
            <when test="column == 'likes'">likes = CASE WHEN likes > 0 THEN likes - 1 ELSE 0 END</when>
            <when test="column == 'favorites'">favorites = CASE WHEN favorites > 0 THEN favorites - 1 ELSE 0 END</when>
            <otherwise>likes = likes</otherwise>
        </choose>
        WHERE id = #{id} AND deleted = 0
    </update>

</mapper>
